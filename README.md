# Node.js Localhost Server 
This is a simple Node.js server that runs on localhost.

## Features
-Runs on port 3000
-Returns JSON response
-Beginner friendly project

## How to Run
1. Install Node.js
2. Run:
   npm install
   node index.js
3. Open:
   http://localhost:3000
